package com.dips.simulator.domain.enums;

public enum FailureScenario {
    VALIDATION_FAIL,
    DEBIT_FAIL,
    CREDIT_FAIL,
    SWITCH_FAIL,
    REVERSAL_FAIL,
    SWITCH_TIMEOUT,
    DEBIT_TIMEOUT,
    CREDIT_TIMEOUT,
    REVERSAL_TIMEOUT,
    NETWORK_TIMEOUT
}

